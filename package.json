{
  "name": "@sdwa/design-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "lint": "biome check --write",
    "lint:ci": "biome ci"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@sdwa/lint-config": "workspace:*"
  }
}
