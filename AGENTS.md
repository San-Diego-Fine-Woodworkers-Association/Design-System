## Agent skills

### Issue tracker

Issues are tracked in Linear (SDFWA Web Infra team, key `SDF`), with Linear Projects used as
epics for `/wayfinder` and similar multi-issue work. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (root `CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.
