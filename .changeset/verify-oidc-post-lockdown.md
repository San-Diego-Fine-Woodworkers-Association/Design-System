---
"@sdwa/lint-config": patch
---

Verify the release pipeline still publishes correctly via OIDC now that this package requires
2FA and disallows classic tokens. No functional change to this package.
